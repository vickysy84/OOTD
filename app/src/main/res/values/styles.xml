<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/ootd_blue</item>
        <item name="colorPrimaryDark">@color/ootd_dark_blue</item>
    </style>

    <!-- Main activity theme. -->
    <style name="OOTDTheme" parent="AppTheme">
    </style>

    <!-- Settings activity theme. -->
    <style name="SettingsTheme" parent="AppTheme">
    </style>

    <style name="ItemListStyle">
        <!-- Here it's a stub.  In screens of sufficient width, this style includes modifications
        for two-pane layout -->
    </style>

</resources>
